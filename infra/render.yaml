services:
  - type: web
    name: niva-ai-core
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
      - key: NIVA_MODE
        value: cloud
